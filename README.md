# Notion Knowledge Graph

개인 Notion 데이터를 **벡터 DB + 그래프 DB**로 변환하여 새로운 인사이트를 발견하는 프로젝트입니다.

```
                 ┌─────────────────────────────────────────────────────┐
                 │                  Notion Knowledge Graph              │
                 │                                                      │
  Notion API     │   ┌─────────┐    ┌─────────┐    ┌─────────────┐    │
       │         │   │ Extract │───▶│  Embed  │───▶│ Build Graph │    │
       ▼         │   └─────────┘    └─────────┘    └─────────────┘    │
  ┌────────┐     │        │              │                │           │
  │ Pages  │─────│────────┘              │                │           │
  │ Blocks │     │                       ▼                ▼           │
  └────────┘     │              ┌──────────────┐  ┌─────────────┐     │
                 │              │   Qdrant     │  │   Neo4j     │     │
                 │              │ (Vector DB)  │  │ (Graph DB)  │     │
                 │              │  1024D BGE   │  │  Page nodes │     │
                 │              └──────────────┘  │  Relations  │     │
                 │                      │         └─────────────┘     │
                 │                      │                │            │
                 │                      └────────┬───────┘            │
                 │                               ▼                    │
                 │                     ┌─────────────────┐            │
                 │                     │ Hybrid Search   │            │
                 │                     │ Vector + Graph  │            │
                 │                     └─────────────────┘            │
                 └─────────────────────────────────────────────────────┘
```

## 주요 기능

- **Notion 데이터 추출**: 모든 페이지와 블록 콘텐츠를 JSON으로 저장
- **벡터 임베딩**: BGE-M3 (다국어 지원)로 1024차원 벡터 생성
- **의미 기반 유사도**: 코사인 유사도로 관련 페이지 자동 연결
- **그래프 시각화**: Neo4j Browser에서 지식 그래프 탐색
- **하이브리드 검색**: 벡터 검색 + 그래프 확장

---

## 아키텍처

### 전체 데이터 흐름

```
┌────────────────────────────────────────────────────────────────────────┐
│                           DATA PIPELINE                                 │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Phase 1: Extract                                                      │
│   ┌──────────┐      ┌───────────────┐      ┌──────────────┐           │
│   │  Notion  │ ───▶ │ notion_export │ ───▶ │  pages.json  │           │
│   │   API    │      │     .py       │      │  (raw data)  │           │
│   └──────────┘      └───────────────┘      └──────────────┘           │
│                                                    │                   │
│   Phase 2: Embed                                   ▼                   │
│   ┌──────────┐      ┌───────────────┐      ┌──────────────┐           │
│   │  BGE-M3  │ ◀─── │ vector_store  │ ◀─── │  pages.json  │           │
│   │  Model   │      │     .py       │      │              │           │
│   └──────────┘      └───────────────┘      └──────────────┘           │
│        │                   │                                           │
│        ▼                   ▼                                           │
│   ┌───────────────────────────┐                                       │
│   │         Qdrant            │                                       │
│   │   ┌─────────────────┐    │                                       │
│   │   │ notion_pages    │    │  • 1024D dense vectors                │
│   │   │ collection      │    │  • Cosine similarity                  │
│   │   └─────────────────┘    │  • Payload: title, url, preview       │
│   └───────────────────────────┘                                       │
│                                                                         │
│   Phase 3: Graph                                                        │
│   ┌──────────────┐     ┌───────────────┐     ┌──────────────┐         │
│   │  pages.json  │ ──▶ │ graph_builder │ ──▶ │    Neo4j     │         │
│   │              │     │     .py       │     │              │         │
│   └──────────────┘     └───────────────┘     └──────────────┘         │
│                                                     │                  │
│   Phase 4: Similarity                               ▼                  │
│   ┌──────────────┐     ┌───────────────┐     ┌──────────────┐         │
│   │    Qdrant    │ ──▶ │  similarity   │ ──▶ │    Neo4j     │         │
│   │   vectors    │     │   _edges.py   │     │  SIMILAR_TO  │         │
│   └──────────────┘     └───────────────┘     └──────────────┘         │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

### 기술 스택

| 구성요소 | 기술 | 역할 |
|---------|------|------|
| **데이터 소스** | Notion API | 페이지/블록 데이터 추출 |
| **임베딩 모델** | BGE-M3 (BAAI) | 다국어 텍스트 → 1024D 벡터 |
| **벡터 DB** | Qdrant | 고속 유사도 검색 |
| **그래프 DB** | Neo4j | 관계 시각화 및 그래프 쿼리 |
| **런타임** | Python 3.9+ | 스크립트 실행 |
| **컨테이너** | Docker Compose | Qdrant, Neo4j 실행 |

### 노드 및 관계 구조

```
┌─────────────────────────────────────────────────────────────┐
│                      GRAPH SCHEMA                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Nodes:                                                      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ (:Page)                                               │   │
│  │   - id: string (Notion page ID)                      │   │
│  │   - title: string                                    │   │
│  │   - url: string                                      │   │
│  │   - wordCount: integer                               │   │
│  │   - blockCount: integer                              │   │
│  │   - createdAt: datetime                              │   │
│  │   - updatedAt: datetime                              │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ (:Date)                                               │   │
│  │   - date: string (YYYY-MM-DD)                        │   │
│  │   - year, month, day: integer                        │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  Relationships:                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ [:SIMILAR_TO]  - 벡터 유사도 기반 (score >= 0.75)    │   │
│  │ [:LINKS_TO]    - Notion 내부 링크                    │   │
│  │ [:CHILD_OF]    - 부모-자식 페이지 관계               │   │
│  │ [:CREATED_ON]  - 생성일 연결                         │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 설치 가이드

### 1. 사전 요구사항

- Python 3.9 이상
- Docker & Docker Compose
- Notion Integration Token ([생성 방법](https://www.notion.so/my-integrations))

### 2. 저장소 클론

```bash
git clone https://github.com/piatoss3612/notion-knowledge-graph.git
cd notion-knowledge-graph
```

### 3. 환경 변수 설정

```bash
cp .env.example .env
```

`.env` 파일 편집:
```env
# Notion API Token (필수)
NOTION_TOKEN=secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Neo4j (필수)
NEO4J_PASSWORD=your_secure_password

# 선택적 설정
SIMILARITY_THRESHOLD=0.75
```

### 4. Docker 서비스 실행

```bash
# Qdrant + Neo4j 시작
docker-compose up -d

# 상태 확인
docker-compose ps
```

| 서비스 | 포트 | 용도 |
|--------|------|------|
| Qdrant | 6333 (REST), 6334 (gRPC) | 벡터 저장/검색 |
| Neo4j | 7474 (Browser), 7687 (Bolt) | 그래프 DB |

### 5. Python 환경 설정

```bash
# 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
```

### 6. Notion Integration 연결

1. [Notion Integrations](https://www.notion.so/my-integrations) 접속
2. "New integration" 클릭
3. 이름 입력, workspace 선택
4. "Internal Integration Token" 복사 → `.env`의 `NOTION_TOKEN`에 붙여넣기
5. Notion에서 공유할 페이지들에 Integration 연결 (페이지 우상단 ··· → Connections → Integration 선택)

---

## 사용 방법

### Phase 1: Notion 데이터 추출

```bash
python scripts/notion_exporter.py
```

**출력 예시:**
```
============================================================
Notion Data Exporter
============================================================
Fetching all pages...
  [1] 프로젝트 관리
  [2] 개발 노트
  ...
Total pages fetched: 1152
Pages: 1152
Total Words: 131,881
Time: 245.3s
```

**생성 파일:** `data/pages.json`

### Phase 2: 벡터 임베딩

```bash
python scripts/vector_store.py
```

**출력 예시:**
```
Loading BGE-M3 model...
Connecting to Qdrant at localhost:6333...
Processing 1152 pages with content...
Embedding: 100%|████████████████| 288/288 [15:32<00:00]

Semantic Search Test
🔍 Query: "프로젝트 관리 방법"
  1. [0.823] 애자일 스프린트 계획
  2. [0.791] 업무 우선순위 결정
```

### Phase 3: 그래프 구축

```bash
python scripts/graph_builder.py
```

**출력 예시:**
```
Creating Page nodes...
Creating Relationships...

📊 Node Statistics:
  Page: 1152
  Date: 242

🔗 Relationship Statistics:
  CREATED_ON: 1152
  LINKS_TO: 203
  CHILD_OF: 44
```

### Phase 4: 유사도 관계 생성

```bash
python scripts/similarity_edges.py
```

**출력 예시:**
```
Creating SIMILAR_TO edges from vector similarity...
Threshold: 0.75 (75%)
✅ Created 1743 SIMILAR_TO relationships
```

### Phase 5: 인사이트 탐색

```bash
# 그래프 인사이트
python scripts/explore_insights.py

# 하이브리드 검색 포함
python scripts/explore_insights.py --hybrid
```

---

## Neo4j 쿼리 가이드

### 기본 접속

1. 브라우저에서 http://localhost:7474 접속
2. Username: `neo4j`, Password: `.env`에서 설정한 값

### 유용한 Cypher 쿼리

#### 1. 전체 그래프 샘플 보기
```cypher
MATCH (p:Page)-[r]->(other)
RETURN p, r, other
LIMIT 50
```

#### 2. 허브 노드 (가장 많은 연결)
```cypher
MATCH (p:Page)-[r:SIMILAR_TO]-(other:Page)
WITH p, count(r) as connections
ORDER BY connections DESC
LIMIT 1
MATCH (p)-[r:SIMILAR_TO]-(connected:Page)
RETURN p, r, connected
LIMIT 30
```

#### 3. 특정 키워드로 페이지 찾기
```cypher
MATCH (p:Page)
WHERE p.title CONTAINS '프로젝트'
RETURN p.title, p.wordCount
ORDER BY p.wordCount DESC
```

#### 4. 강한 유사도 클러스터 (85% 이상)
```cypher
MATCH (p1:Page)-[r:SIMILAR_TO]->(p2:Page)
WHERE r.score > 0.85
RETURN p1.title, p2.title, r.score
ORDER BY r.score DESC
LIMIT 20
```

#### 5. 고립된 페이지 (연결 없음)
```cypher
MATCH (p:Page)
WHERE NOT (p)-[:SIMILAR_TO|LINKS_TO|CHILD_OF]-()
AND p.wordCount > 100
RETURN p.title, p.wordCount
ORDER BY p.wordCount DESC
```

#### 6. 월별 생성 추이
```cypher
MATCH (p:Page)-[:CREATED_ON]->(d:Date)
WHERE d.year >= 2024
RETURN d.year, d.month, count(p) as pages
ORDER BY d.year, d.month
```

#### 7. 특정 페이지 주변 탐색
```cypher
MATCH (center:Page {title: "프로젝트 관리"})
OPTIONAL MATCH (center)-[r:SIMILAR_TO]-(similar:Page)
RETURN center, r, similar
```

#### 8. 2-depth 연결 탐색
```cypher
MATCH path = (start:Page {title: "개발 가이드"})-[:SIMILAR_TO*1..2]-(related:Page)
RETURN path
LIMIT 50
```

#### 9. 페이지 계층 구조
```cypher
MATCH path = (child:Page)-[:CHILD_OF*]->(parent:Page)
WHERE NOT ()-[:CHILD_OF]->(child)
RETURN path
LIMIT 20
```

#### 10. 콘텐츠가 풍부한 페이지
```cypher
MATCH (p:Page)
WHERE p.wordCount > 1000
RETURN p.title, p.wordCount, p.blockCount
ORDER BY p.wordCount DESC
LIMIT 10
```

---

## 프로젝트 구조

```
notion-knowledge-graph/
├── README.md                 # 이 파일
├── docker-compose.yml        # Qdrant + Neo4j 설정
├── requirements.txt          # Python 의존성
├── .env.example             # 환경 변수 템플릿
├── .gitignore
│
├── scripts/
│   ├── notion_exporter.py   # Phase 1: Notion → JSON
│   ├── vector_store.py      # Phase 2: JSON → Qdrant
│   ├── graph_builder.py     # Phase 3: JSON → Neo4j
│   ├── similarity_edges.py  # Phase 4: Qdrant → Neo4j (SIMILAR_TO)
│   └── explore_insights.py  # Phase 5: 인사이트 탐색
│
└── data/                    # (git 제외) 추출된 데이터
    ├── pages.json
    └── databases.json
```

---

## 향후 계획

- [ ] **개념 노드 추출**: LLM으로 페이지에서 주요 개념 추출 → Concept 노드로 연결
- [ ] **MCP 서버**: Claude Code와 직접 연동
- [ ] **자동 동기화**: Notion 변경 시 자동 업데이트 (Webhook)
- [ ] **Neo4j Bloom**: 고급 시각화 대시보드

---

## 라이선스

MIT License

---

## 기여

Issue와 PR 환영합니다!
